To Run:
# To run, call ./CreateAngularProject.sh in the script directory (for now)
# Should already be executable (chmod +x CreateAngularProject.sh)
# Requires NPM to already be installed

Known working case:
	App name = HelloAngular
	Route #1 = hello
	Page #1 = World
